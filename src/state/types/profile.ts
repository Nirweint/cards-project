import {SomeActionType} from "../actions/profile";

export type ProfileStateType = {}

export enum ProfileAction {
    SOME_ACTION = "profileReducer/SOME_ACTION"
}

export type ProfileActionsType = SomeActionType

