import React from 'react';
import {Header} from "../header";
import {RoutesComponent} from "../routes";

export const Main = () => {
    return (
        <div>
            <Header/>
            <RoutesComponent/>
        </div>
    );
}